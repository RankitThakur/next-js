'use client'

export default function Dashboard() {
    return (
        <main className="flex min-h-screen flex-col items-center justify-between p-24">
            <button onClick={() => alert("i am here dashboard component")}>Click me</button>
        </main>
    )
}
